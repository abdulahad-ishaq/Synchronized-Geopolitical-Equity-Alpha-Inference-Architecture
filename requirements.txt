# ───────────────────────────────────────────────────────────────────────────
# Synchronized Geopolitical-Equity Alpha Inference Architecture
# requirements.txt  |  Python >= 3.10
# ───────────────────────────────────────────────────────────────────────────

# ── Core scientific stack ────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.1.0
scikit-learn>=1.4.0

# ── Deep learning ────────────────────────────────────────────────────────
torch>=2.2.0                     # CPU build; for CUDA see NOTE 1 below
transformers>=4.40.0             # HuggingFace hub  (FinBERT-Tone)
sentencepiece>=0.2.0             # required by some HF tokenisers
accelerate>=0.30.0               # optional but suppresses HF warnings

# ── Market data ──────────────────────────────────────────────────────────
yfinance>=0.2.40
pandas-datareader>=0.10.0        # FRED macro series

# ── HTTP / REST ───────────────────────────────────────────────────────────
requests>=2.31.0

# ── Streamlit + visualisation ────────────────────────────────────────────
streamlit>=1.35.0
plotly>=5.22.0

# ── Utilities ────────────────────────────────────────────────────────────
python-dotenv>=1.0.0             # load .env for local/CLI development

# ───────────────────────────────────────────────────────────────────────────
# NOTES
# ───────────────────────────────────────────────────────────────────────────
#
# NOTE 1 – GPU / CUDA:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#
# NOTE 2 – FinBERT model weight download (~440 MB):
#   Happens automatically on first run via HuggingFace Hub.
#   @st.cache_resource ensures it loads only ONCE per Streamlit session.
#
# NOTE 3 – API keys (Streamlit Cloud):
#   In your app's Settings → Secrets, add:
#       NEWSAPI_KEY = "your_key_here"
#       FRED_API_KEY = "your_key_here"   # optional
#   Keys are read via st.secrets in the app; os.environ fallback for CLI.
#
# NOTE 4 – Streamlit Cloud memory:
#   The free tier has 1 GB RAM. With FinBERT frozen (~440 MB) + Streamlit
#   overhead, you may need the "Starter" plan ($) for comfortable headroom.
#   Alternatively, quantise the model: pip install optimum and use
#   ORTModelForSequenceClassification for ~4× smaller footprint.
#
# NOTE 5 – Claude Code CLI usage:
#   npm install -g @anthropic-ai/claude-code
#   export ANTHROPIC_API_KEY="..."
#   export NEWSAPI_KEY="..."
#   claude   # then prompt: "run python data_loader.py"
# ───────────────────────────────────────────────────────────────────────────
